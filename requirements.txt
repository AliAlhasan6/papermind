# Core agent + LLM (local via Ollama)
langgraph>=1.0,<2.0  # create_react_agent removed in v2.0 — see agent.py
langchain-ollama>=0.2.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0

# Retrieval
chromadb>=0.5.0
sentence-transformers>=3.0.0

# PDF + KG
pypdf>=4.0.0
networkx>=3.0

# Data validation + UI + env
pydantic>=2.0
gradio>=4.0.0
python-dotenv>=1.0.0

# Dev
pytest>=8.0.0
ruff>=0.5.0
